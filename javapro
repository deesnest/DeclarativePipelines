pipeline {
    agent {label "Agent Java"}
    stages {
        stage('build1') {
            steps {
                sh '''
                 
                  ls -l
                cd java-tomcat-maven-example
                mvn install
                  
                 '''
             }
        }
        
    }
}
