apiVersion: 
kind: Service 
metadata:
  name: my-python
spec:
  selector: 
    app: nginx
    type: ClusterIP
    ports:
      - name: http
        port: 30081
        targetPort: 80
        protocol: TCP	
restartPolicy: Always
